# إصدار Signal Grid — ملخص الإطلاق

**AZ Alpha Vision** أصبح يستخدم في هذا الإصدار لوحة Signal Grid الداكنة، وهي واجهة أصلية للمحاكي التعليمي تجمع السوق والإشارة والمحفظة والترشيحات والأخبار والأرباح والمساعد في مساحة واحدة قابلة للتخصيص.

## ما أُنجز في هذا الإصدار

| المكوّن | الحالة | الوصف |
|---|---:|---|
| لوحة Signal Grid | جاهز | ثماني وحدات منظمة داخل شبكة متجاوبة: السوق، الإشارة، المحفظة، الترشيحات، AZ ai، الأرباح، الأخبار، وقائمة المراقبة. |
| تخصيص المستخدم | جاهز | يمكن سحب الوحدات وإعادة ترتيبها، وتغيير حجم كل وحدة. يُحفظ الترتيب في المتصفح بشكل منفصل لكل مستخدم. |
| بيانات المحاكي | محفوظ | تعرض وحدة المحفظة القيم الفعلية من المحاكي المشترك، ولا تنشئ بيانات أو نتائج اصطناعية. |
| ترشيحات الأسبوع | مربوط | تعرض الوحدة أفضل الترشيحات المحفوظة، وتتحدث عند اكتمال مسح الأسبوع أو عند حذف بيانات قديمة غير موثوقة. |
| الأخبار والأرباح | مربوط | تنسخ الوحدة العرض المختصر من المصدرين الحاليين بعد تحميل الأخبار وتقويم الأرباح، ولا تغير منطق جمع البيانات أو مصدرها. |
| AZ ai | مربوط | زر الوحدة يفتح المساعد الحالي مباشرة. |
| استجابة الهاتف | جاهز | تتحول الشبكة إلى عمود واحد، وينتقل التنقل إلى شريط سفلي داكن. |
| أمان البيانات | محفوظ | لا توجد مفاتيح جديدة في الواجهة، ولا تغير هذه الدفعة سياسات Supabase أو وظائف الإشعار. |
| اختبار Push موجّه | جاهز | يظهر حساب المدير نفسه الآن ضمن «مستخدم محدد» باسم «حسابك»، حتى يمكن إجراء اختبار آمن بلا إرسال جماعي. |

## التحقق المنفذ

تم تنفيذ فحص JavaScript لملفي `app.js` و`sw.js`، والتحقق من وجود معرّفات الوحدات الجديدة، ومعاينة سطح المكتب محليًا. ظهر في المعاينة الشريط الجانبي الداكن والخلفية الداكنة والوحدات الثمانية. كما تم اختبار زر تغيير حجم وحدة، وثبت أن التفضيل يُحفظ محليًا.

## ما لا يدّعيه هذا الإصدار

هذا الإصدار لا يحول بيانات التحديث الدوري إلى أسعار لحظية بالثانية، ولا يعرض دفتر أوامر أو شموعًا حقيقية دون مصدر OHLC موثوق. لذلك تبقى حالة الرسم البياني صادقة إلى أن تتوفر البيانات المطلوبة. كما أن وجود وحدة الأخبار أو الأرباح لا يعني وصول بيانات جديدة قبل تشغيل مجامع البيانات الخلفية بنجاح.

## X والدفع

| التكامل | الوضع الحالي | الشرط قبل الإطلاق |
|---|---|---|
| X | توجد بنية تسويق موحدة في المشروع السابق، لكن لا يوجد اتصال X مؤكد ومهيأ في هذه الجلسة. | نحتاج بيانات اعتماد مشروع X/Render الفعلية وموافقة صريحة قبل النشر أو إرسال أي منشور. |
| الدفع | شاشة الباقات والتحويل البنكي موجودة ولا تخزن بطاقات داخل الموقع. | بوابة دفع حية مثل Stripe أو مزود محلي تحتاج حساب تاجر ومفاتيح وخطاف Webhook آمن. لا يجب إدخال مفاتيح دفع في GitHub أو JavaScript. |

> لا يتم نشر منشور، أو تحصيل مبلغ، أو ربط حساب X أو بوابة دفع دون موافقة المالك وخطوة اختبار منفصلة.

## ملفات النشر لهذه الدفعة

ارفع إلى **جذر GitHub** مع الاستبدال:

```text
index.html
app.js
sw.js
manifest.json
icon.svg
icon-192.png
icon-512.png
```

لا تتطلب هذه الدفعة SQL أو Secrets جديدة أو إعادة نشر وظائف Supabase.

## الخطوة التالية بعد النشر

بعد أن تظهر الواجهة، يُختبر أولًا تبديل التبويب وسحب وحدة وتغيير حجمها، ثم ينفذ اختبار Push موجّه إلى حساب المدير نفسه مع إغلاق تبويب التطبيق على جهاز الاستقبال. بعد ذلك ينفذ مسح أسبوعي حقيقي للتحقق من ظهور الترشيحات والأخبار والأرباح داخل الوحدات. وبعد اعتماد الواجهة نبدأ تكامل X والدفع كمشروعين خارجيين مضبوطين، لا كتعديلات واجهة متفرقة.

*إعداد: Manus AI — 2026*
